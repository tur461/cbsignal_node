const {
    d_act, 
    p_act
}                   = require('./actions');
const {
    d_type,
    p_type,
    s_d_type,
    s_p_type,
}                   = require('./types');
const {
    ns,
}                   = require('./common');


module.exports = {
    ns,
    d_act,
    p_act,
    d_type,
    p_type,
    s_d_type,
    s_p_type,
}